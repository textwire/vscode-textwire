export const DIRECTIVE_NAMES = [
    '@if',
    '@if @else',
    '@if @elseif',
    '@use',
    '@insert',
    '@insert @end',
    '@reserve',
    '@component',
    '@component @slot',
    '@slot',
    '@slot(name)',
    '@each',
    '@each @else',
    '@for',
    '@for @else',
    '@dump',
    '@end',
    '@break',
    '@continue',
    '@continueIf',
    '@breakIf',
]
